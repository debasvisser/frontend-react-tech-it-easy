// function productName(product) {
//     return `${product.brand} ${product.type} - ${product.name}`;
// }

const productName = (product) => `${product.brand} ${product.type} - ${product.name}`;

export default productName;